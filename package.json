{
  "name": "css-layouting-portofolio-scss",
  "version": "1.0.0",
  "private": true,
  "description": "Konversi CSS ke SCSS (7-1 pattern) - Tugas Rutin 4",
  "scripts": {
    "sass:build": "sass main.scss dist/style.css --no-source-map",
    "sass:watch": "sass --watch main.scss:dist/style.css",
    "dev": "vite",
    "build": "vite build"
  },
  "devDependencies": {
    "sass": "^1.77.0",
    "vite": "^5.4.0"
  }
}
